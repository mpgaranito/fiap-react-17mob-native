import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        },
      title: {
        fontSize: 19,
        fontWeight: 'bold',
      },
      activeTitle: {
        color: 'red',
      },
});